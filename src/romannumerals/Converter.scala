
package romannumerals

//import Numerals._

object Converter {

  //val numerals= List(I,V,X,L,C,D,M).toMap {_.symbol}

  def convert(numeralStr: String): Long = {
    //val numeralList = Lists.fromString(numeralStr).map { numerals(_) }
    //print(numeralList)
    println("convert")
    0
  }
}
